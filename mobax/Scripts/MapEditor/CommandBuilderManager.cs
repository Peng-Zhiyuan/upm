//using Drawing;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CommandBuilderManager : MonoInstance<CommandBuilderManager>
{
    //DrawingData gizmos;
    //Dictionary<string, CommandBuilder> CommandBuilderMap = new Dictionary<string, CommandBuilder>();
    //public CommandBuilder FetchBuilder(string id)
    //{
    //    if (!CommandBuilderMap.ContainsKey (id)) {
    //        if (gizmos == null) gizmos = new DrawingData();
    //        var cmdBuilder = gizmos.GetBuilder(false);
    //        CommandBuilderMap.Add(id, cmdBuilder);
    //    }

    //    return CommandBuilderMap[id];
    //}

    void Update()
    {
        
    }
}
