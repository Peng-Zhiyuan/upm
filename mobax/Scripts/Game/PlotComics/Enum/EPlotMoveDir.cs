using Sirenix.OdinInspector;

namespace Plot.Runtime
{
    public enum EPlotMoveDir
    {
        [LabelText("向前")] Froward,
        [LabelText("向后")] Back,
        [LabelText("向左")] Left,
        [LabelText("向右")] Right
    }
}