using Sirenix.OdinInspector;

namespace Plot.Runtime
{
    public enum EPlotComicsModelType
    {
        [LabelText("角色")] Hero,
        [LabelText("怪物")] Monster,
        [LabelText("猫咪")] Cat,
    }
}